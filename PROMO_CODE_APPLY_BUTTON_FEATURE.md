# تحديث: زر تطبيق كود الخصم مع معاينة السعر

## الميزات الجديدة

### 1. **زر "تطبيق" بجانب حقل كود الخصم**
- يظهر زر "تطبيق" بجانب حقل إدخال كود الخصم
- الزر يكون غير نشط إذا لم يتم اختيار طريقة الشحن (لأننا نحتاج السعر الأساسي)
- عند الضغط عليه، يتم التحقق من صحة الكود

### 2. **معاينة السعر قبل تأكيد الطلب**
عند تطبيق كود خصم صحيح، يظهر صندوق أخضر يعرض:
- ✅ رسالة نجاح: "تم تطبيق الخصم بنجاح!"
- السعر الأصلي (مشطوب)
- نسبة الخصم ومبلغ الخصم
- السعر النهائي بعد الخصم (بخط عريض وبارز)

### 3. **معالجة الأخطاء**
- إذا كان الكود غير صحيح أو منتهي الصلاحية، يظهر رسالة خطأ تحت حقل الإدخال
- إذا كان الحقل فارغ، يظهر رسالة "الرجاء إدخال كود الخصم"
- عند حدوث خطأ في الاتصال، يظهر رسالة "حدث خطأ أثناء التحقق من الكود"

### 4. **حالة التحميل**
- عند الضغط على زر "تطبيق"، يظهر مؤشر تحميل دائري
- الزر يصبح غير نشط أثناء التحميل

### 5. **إعادة التعيين التلقائي**
- عند تعديل كود الخصم بعد التطبيق، يتم إعادة تعيين حالة التحقق تلقائياً
- يختفي صندوق معاينة السعر

---

## الملفات المطلوبة

### 1. SQL Function في Supabase

يجب تنفيذ الـ SQL function التالية في Supabase SQL Editor:

**الملف:** `validate_promo_code_function.sql`

```sql
CREATE OR REPLACE FUNCTION public.validate_promo_code(
  p_promo_code text,
  p_base_price numeric
)
RETURNS TABLE (
  is_valid boolean,
  percentage numeric,
  discount_amount numeric,
  final_price numeric,
  error_message text
)
LANGUAGE plpgsql
AS $$
DECLARE
  v_promo_id uuid;
  v_percentage numeric;
  v_discount numeric := 0;
  v_final_price numeric;
BEGIN
  -- التحقق من كود الخصم
  SELECT id, percentage
  INTO v_promo_id, v_percentage
  FROM public.promo_codes
  WHERE code = p_promo_code
    AND is_active = true
    AND (expires_at IS NULL OR expires_at > now());

  -- إذا لم يتم العثور على الكود أو غير صالح
  IF v_promo_id IS NULL THEN
    is_valid := false;
    percentage := 0;
    discount_amount := 0;
    final_price := p_base_price;
    error_message := 'كود الخصم غير صالح أو منتهي الصلاحية';
    RETURN NEXT;
    RETURN;
  END IF;

  -- حساب الخصم
  v_discount := (p_base_price * v_percentage) / 100;
  v_final_price := p_base_price - v_discount;

  -- إرجاع النتيجة
  is_valid := true;
  percentage := v_percentage;
  discount_amount := v_discount;
  final_price := v_final_price;
  error_message := NULL;
  
  RETURN NEXT;
END;
$$;

GRANT EXECUTE ON FUNCTION public.validate_promo_code(text, numeric) TO authenticated;
```

---

## خطوات التنفيذ

### الخطوة 1: تنفيذ SQL Function
1. افتح Supabase Dashboard
2. اذهب إلى SQL Editor
3. انسخ والصق الكود من ملف `validate_promo_code_function.sql`
4. اضغط Run

### الخطوة 2: اختبار الميزة
1. شغل التطبيق
2. أضف منتجات للسلة
3. اذهب لشاشة إنشاء الطلب
4. اختر العنوان وطريقة الشحن
5. أدخل كود خصم صحيح
6. اضغط زر "تطبيق"
7. تحقق من ظهور معاينة السعر

---

## سيناريوهات الاختبار

### ✅ سيناريو 1: كود خصم صحيح
1. أدخل كود خصم نشط وصالح
2. اضغط "تطبيق"
3. **النتيجة المتوقعة:** 
   - يظهر صندوق أخضر
   - السعر الأصلي مشطوب
   - يظهر مبلغ الخصم ونسبته
   - يظهر السعر النهائي

### ❌ سيناريو 2: كود خصم غير صحيح
1. أدخل كود غير موجود (مثل: "INVALID123")
2. اضغط "تطبيق"
3. **النتيجة المتوقعة:**
   - رسالة خطأ تحت الحقل: "كود الخصم غير صالح أو منتهي الصلاحية"
   - لا يظهر صندوق معاينة السعر

### ❌ سيناريو 3: كود خصم منتهي الصلاحية
1. أدخل كود منتهي الصلاحية
2. اضغط "تطبيق"
3. **النتيجة المتوقعة:**
   - رسالة خطأ: "كود الخصم غير صالح أو منتهي الصلاحية"

### ⚠️ سيناريو 4: حقل فارغ
1. اترك حقل كود الخصم فارغاً
2. اضغط "تطبيق"
3. **النتيجة المتوقعة:**
   - رسالة خطأ: "الرجاء إدخال كود الخصم"

### 🔄 سيناريو 5: تعديل الكود بعد التطبيق
1. طبق كود خصم صحيح
2. عدل الكود في الحقل
3. **النتيجة المتوقعة:**
   - يختفي صندوق معاينة السعر
   - يتم إعادة تعيين حالة التحقق

---

## ملاحظات مهمة

1. **السعر الأساسي:** يتم حسابه من `PricingCubit` بناءً على:
   - أسعار المنتجات في السلة
   - طريقة الشحن المختارة
   - إعدادات التسعير من قاعدة البيانات

2. **التحقق من الكود:** يتم على الخادم فقط - لا توجد معالجة محلية

3. **الأمان:** الـ RPC function تتحقق من:
   - وجود الكود في قاعدة البيانات
   - أن الكود نشط (`is_active = true`)
   - أن الكود غير منتهي الصلاحية

4. **تجربة المستخدم:**
   - الزر غير نشط حتى يتم اختيار طريقة الشحن
   - مؤشر تحميل أثناء التحقق
   - رسائل واضحة للأخطاء
   - معاينة مرئية للسعر قبل التأكيد
